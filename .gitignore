node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.env*.local

# Vendored AGPL source (ERPNext + Frappe) — design reference only, not shipped
reference/

# Ask BIMS local test harness logs
tests/ask-bims/acceptance-run-*.log

# Ask BIMS generated answer sheets for human grading (output, not source)
docs/ask-bims-acceptance-answers-*.md

# Python cache + venv for services/bims-capture-agent/ (Pi-side service)
__pycache__/
*.pyc
services/bims-capture-agent/.venv/
services/bims-capture-agent/.env
