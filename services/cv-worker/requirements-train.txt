# Minimal deps for the CI trainer (train_cli.py). No boto3/R2 SDK — the runner
# uses plain HTTP (urllib) for image download + model upload, so it needs no R2
# creds. Excludes the FastAPI/uvicorn/opencv/onnxruntime bits the long-lived
# worker (main.py) needs. anomalib pulls torch + pillow + numpy transitively.
torch>=2.2.0
anomalib>=1.0.0
