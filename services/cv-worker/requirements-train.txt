# Minimal deps for the CI trainer (train_cli.py). Excludes the FastAPI/uvicorn/
# opencv/onnxruntime bits the long-lived worker needs but the one-shot CLI does
# not — keeps the GitHub Actions install fast. anomalib pulls torch + pillow +
# numpy transitively.
torch>=2.2.0
anomalib>=1.0.0
boto3>=1.34.0
