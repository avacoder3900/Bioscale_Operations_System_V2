# Files deployed to lab Linux devices (OT-2 bridge daemon, Pi agents) must
# keep LF endings — a CRLF checkout on Windows breaks shebangs and `sh run.sh`
# when the files are copied to the robot.
*.py text eol=lf
*.sh text eol=lf
*.service text eol=lf
