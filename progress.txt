## Codebase Patterns

### Server File Pattern
Every `+page.server.ts` follows:
1. Import: `connectDB`, models, `requirePermission`
2. Guard: `requirePermission(event, 'domain:read|write')`
3. Connect: `await connectDB()`
4. Query: Mongoose `.find()`, `.findById()`, `.aggregate()` with `.lean()`
5. Serialize: `JSON.parse(JSON.stringify(result))`
6. Return: exact shape from CONTRACT-REGISTRY.md

### Auth Pattern
- `event.locals.user` — authenticated user (set by hooks.server.ts)
- `event.locals.session` — session object
- `requirePermission(event, 'permission:string')` — throws 403 if missing
- `isAdmin(event)` — boolean check
- Cookie: `auth-session`, looked up in `sessions` MongoDB collection

### ID Generation
```typescript
import { generateId } from '$lib/server/db/models';
const id = generateId(); // nanoid string
```

### Denormalization Pattern
At write time, embed operator/user/project info:
```typescript
createdBy: { _id: user._id, username: user.username }
project: { _id: project._id, name: project.name, color: project.color }
```

### Sacred Document Pattern
- Check `finalizedAt` before any mutation
- Use `corrections[]` array for post-finalization fixes
- Write-once phases on CartridgeRecord: check `phase.recordedAt: { $exists: false }`

### Immutable Log Pattern
- AuditLog, ElectronicSignature, InventoryTransaction, DeviceEvent, ManufacturingMaterialTransaction
- Append only — never update or delete
- Mongoose middleware blocks `.updateOne()`, `.deleteOne()`, etc.

### API Auth Pattern (Agent API)
```typescript
const apiKey = event.request.headers.get('x-api-key')
    || event.request.headers.get('x-agent-api-key');
if (apiKey !== process.env.AGENT_API_KEY) return json({ error: 'Unauthorized' }, { status: 401 });
```

---

## Weekend Build Log (Feb 27 – Mar 2, 2026)

### Project Genesis
- Workshopped full rebuild plan with Jacob Quick
- Decision: greenfield rebuild (not in-place migration) — old Postgres app stays untouched
- Three-layer architecture: UI (frozen), Data Contracts (preserved), Server+DB (rebuilt)

### Phase 1: Characterization Test Suite (Feb 27)
- Built 84 HTTP-level contract tests against running Postgres app
- Tests verify: page load data shapes, API response shapes, auth flow, business logic
- Tests are database-agnostic — same tests run against Postgres or MongoDB
- Location: `tests/contracts/` (11 test files by domain)
- Test user: `contracttest` / `contracttest123`
- All 84 passing (green baseline)

### Phase 2: Contract Registry (Feb 27)
- Extracted data contracts from all ~80 routes
- Documented: load() return shapes, form action inputs/outputs, API endpoints, layout data
- Inventoried 100+ shared server utilities
- Output: `docs/migration/CONTRACT-REGISTRY.md` (3,752 lines)

### Phase 2.5: PRDs (Feb 27)
- Wrote 11 domain PRDs with 40 total stories
- Each story: routes covered, contract refs, MongoDB models, acceptance criteria
- Domain order: Auth → Kanban → Customers → Equipment → Documents → Inventory → SPU → Manufacturing → Shipping → Cartridge Lab → Agent API
- Output: `docs/migration/prds/` (11 files + prd.json)

### Phase 3: Greenfield Build (Feb 27–28)

#### 3.0: Project Setup + Auth (commit e69b7e3 → 1ea69c9)
- Scaffolded new SvelteKit project
- Copied 131 .svelte files (UI layer — untouched)
- Created all 53 Mongoose models with indexes, sacred middleware, immutable middleware
- Implemented hooks.server.ts (session validation, cookie management)
- Built auth utilities: session create/validate/invalidate, permission checking
- Login/logout, user CRUD, role management, invite flow
- Seed script: 3 users (admin, operator, viewer), 3 roles
- Verified: login works, session cookies set, route protection active

#### 3.1: Kanban + Customers (commit ca5f90d)
- Board view, projects CRUD, task detail with comments/tags/proposals
- List view with filters, archived tasks
- Customer CRUD with embedded notes
- Drag-and-drop status updates via API endpoint

#### 3.2: Equipment + Documents (commit ca5f90d, 47c413f)
- Equipment CRUD (fridges, ovens), locations, decks/trays (consumables)
- Opentrons robots with live HTTP health checks
- Controlled documents with revisions, approval workflow, e-signatures
- Work instructions with nested versions → steps → requirements
- Document repository, file upload
- Box.com integration: stubbed (deferred)

#### 3.3: Inventory + SPU (commit 4ee982d)
- Part definitions CRUD, BOM items with version history
- Inventory transactions (immutable ledger)
- SPU dashboard with filtering, detail page with full build record
- Assembly sessions: step-by-step flow, barcode scanning, field capture
- Assembly completion: copies full session into SPU (sacred document)
- Production runs, batches, validation sessions
- Atomic barcode generation via findOneAndUpdate + $inc

#### 3.4: Manufacturing (commit 7003749)
- Lot records (operational — mutable), process configurations
- Wax filling: runs, cartridge phases (WRITE-ONCE), QC inspection, storage
- Reagent filling: batch records (sacred), tube prep, inspection, top seal
- Assay definitions (sacred, lockable): CRUD, version history, lock/unlock
- QA/QC release, manufacturing materials (immutable transactions), laser cutting
- CartridgeRecord lifecycle: backing → waxFilling → waxQc → waxStorage → reagentFilling → reagentInspection → topSeal → ovenCure → storage → qaqcRelease → shipping

#### 3.5: Shipping + Cartridge Lab + Agent API (commit 9559f2f)
- Shipping lots with QA/QC releases, packages with full customer snapshot
- Lab cartridges with usage log, cartridge groups
- Firmware devices/cartridges, test results with spectro readings
- Agent API: health, schema, queries, messages, operations, approvals (API key auth)

### Phase 4: Verification (Feb 28)
- Ran all 84 contract tests against new MongoDB app
- Fixed 18 failures:
  - 1 wrong data shape (parts key name)
  - 15 missing API sub-routes (consolidated → individual)
  - 2 auth header mismatches (X-API-Key vs x-agent-api-key)
- Final result: 84/84 passing ✅
- Build passes ✅

### Phase 4 Manual Verification (pending)
- Jacob to spot-check critical workflows
- App needs real data (currently seed data only) or migration from Postgres

### Known Issues
- `svelte-check` reports 834 errors — pre-existing from copied UI layer, not caused by migration
- Old app used argon2 for passwords; new app uses bcrypt — users need new passwords
- Box.com integration is stubbed (deferred)
- Opentrons endpoints depend on live robot hardware
- MongoDB has seed data only — no production data migrated yet

### 2026-07-13: Architecture survey + performance audit (Claude session)
- Surveyed full stack → wrote `BIMS_ARCHITECTURE.md` (data structures, storage,
  cloud infra, frontend) via 3 parallel exploration agents
- Ran read-side + write-side performance audits (361 find calls, form actions,
  serialization, indexes) → appended "Performance Audit" section to BIMS_ARCHITECTURE.md
- Key findings:
  - P0 BUG: spu/[spuId] AuditLog query uses non-existent fields (entityId/createdAt
    vs recordId/changedAt) → full audit_log scan on every SPU view, returns nothing
  - P0 BUG: CartridgeRecord.phases $push targets undeclared schema field (strict
    mode silently drops) — shipping/+page.server.ts:187
  - ~7 missing indexes (Spu.createdAt, TestResult, InventoryTransaction,
    AssemblySession.spuId, ElectronicSignature, LabCartridge, CartridgeRecord.createdAt)
  - spu/mfg loads ALL SPUs unprojected (incl. inline CSVs) for a 9-field list
  - receiving/new does 6-8 serialized writes; audit-log write serialized in 50+ actions
  - cartridge updateStatus/changeGroup: read-modify-write race + unbounded usageLog[]
  - connection.ts not hardened for serverless (boolean guard, minPoolSize 1)
- Wrote PRD: docs/prds/PERF-01-data-read-write-health.md
- Started perf rework branch: perf/data-health-01 (multi-agent workflow implementing
  P0 bugs, indexes, projections, parallelization, race fix, connection hardening)
- Workflow completed (14 agents): all 8 PERF-01 tasks implemented; adversarial review
  found 6 findings; critical (magnetometer Promise.all vs sacred middleware ordering
  -> phantom immutable audit entries) and major (LabCartridge.status enum bypassed by
  updateOne) both fixed
- R6 DECISION (deliberate, per PRD flag requirement): usageLog $push left UNCAPPED —
  cartridge detail page (cartridges/[cartridgeId]) renders the FULL usage history, so
  a $slice cap would silently truncate displayed data; race fix ($set+$push atomic) still
  applied. Capping requires a paginated usage-history read first (PERF-02 candidate)
- Known cosmetic nit (left as-is): spu/[spuId] audit-trail fallback now shows raw
  changedBy string for deleted-user IDs / literal 'system' instead of 'System'

### 2026-07-13 (later session): PERF-01 follow-ups via parallel agents (perf/data-health-01, uncommitted)
- Resumed cancelled session's work; 6 parallel agents on the deferred items:
- Pagination (50/page + countDocuments + Prev/Next pager UI): spu/mfg, assays,
  customers, documents/instructions, spu/documents/repository,
  manufacturing/inventory, manufacturing/consumables
- Skipped w/ rationale: laser-cutting + equipment/activity (already capped at 50),
  cartridges/analysis (needs DB aggregation rewrite, not a pager), parts (bounded
  master data; UI filters need full set)
- mfg scan-bar regression fixed: lookup now resolves server-side (?lookup= ->
  exact-match query -> redirect); register offer only on true DB miss
- test-results/[resultId]: readings[] mapped output slimmed 17->14 fields
  (dropped position/laserOutput/timestampMs; UI-verified unused) ~18% payload cut
- Audit-write rollout: 28/49 sites are primary+single-audit -> correctly left
  sequential (phantom-audit rule); real wins: api/agent/messages (counter batched
  w/ audit), api/agent/query/seed (~60 upserts Promise.all'd); e-signature flows +
  thermocouple deliberately untouched
- Punch list (open): consumables+inventory summary cards now page-scoped;
  customers search still server-ignored (pre-existing); repository columns broken
  (pre-existing); assays stat-card full scan; analysis-page aggregation rewrite;
  phases[] backfill decision
- Typecheck: 183 errors = exact pre-existing baseline, zero new (first run showed
  +30, artifact of skipping svelte-kit sync). Production build: in progress.
- DEPLOYMENT 2026-07-13: committed b971b8bf on perf/data-health-01, pushed to
  origin (avacoder3900/Bioscale_Operations_System_V2) -> Vercel preview build
  pending; local prod build unusable on this machine (30GB native alloc crash,
  machine-level; Vercel is the gate). Preview URL to be logged when status lands.
- DEPLOYMENT RESULT: b971b8bf failed on Vercel (untracked QMS files: qms-gate.ts,
  qms.ts, qms-state.ts + admin/qms & photo-staging routes were imported but never
  committed); fixed in 2b469f46 -> Vercel build SUCCESS. Preview:
  https://bioscale-operations-system-mongodb-git-perf-da-5173e4-brevitest.vercel.app
  (branch perf/data-health-01). Local 30GB build crash confirmed machine-only.
- BOX INVENTORY DISCONNECT 2026-07-30: Box is no longer the inventory source.
  Deleted src/lib/server/box-sync.ts (syncPartsFromBox ran deleteMany({}) on
  part_definitions then re-upserted from "Live Equipment Overview.xlsx", so any
  manual edit was destroyed on the next sync). Removed ?/sync action + boxStatus
  + syncErrorDetail from /spu/parts/+page.server.ts, and the Sync-from-Box /
  Connect-to-Box buttons, Last Sync card and retry-sync form from
  /spu/parts/+page.svelte. Box FILE STORAGE deliberately kept: box.ts,
  /api/box/upload, /api/box/files/[id], documents browser, receiving CoC uploads
  all verified unchanged. part_definitions (69 rows) NOT modified - kept as-is,
  now BIMS-owned master data edited directly via mongosh.
- Context: Box sync was already dead - lastSyncAt 2026-03-04, lastSyncStatus
  "partial", E11000 dup key on partNumber:null, access token expired 2026-03-20.
- Tooling: mongosh 2.9.2 installed via winget at
  C:\Users\aleja\AppData\Local\Programs\mongosh\mongosh.exe (not on PATH; use
  full path). Target = cluster0.vmsozkl.mongodb.net db "bioscale" (PROD, single
  DB, .env and .env.local are identical - no dev/prod split).
- LANDMINE: PartDefinition Mongoose schema (strict) does NOT declare bomType,
  isBom, quantityPerUnit, inventorySource, supplierPartNumber, barcode - all of
  which exist on 44-69 of the 69 docs. Any app-side .save() SILENTLY STRIPS them
  (same class as the CV strict-mode incident). Edit part_definitions via mongosh,
  not via Mongoose, until the schema is widened.
- Open items deliberately NOT actioned (user decided): (1) /spu/bom/settings
  +page.server.ts lines 30-42 returns boxIntegration incl. accessToken AND
  refreshToken to the CLIENT - SvelteKit serializes load data into the page
  payload, so any inventory:read user can read both from view-source; the .svelte
  never references boxIntegration, so it is dead code and safe to delete.
  (2) /spu/parts load filters to unitCost > 0, hiding 8 parts; PT-CT-111 (35),
  PT-CT-112 (305), PT-CT-114 (17) hold real stock but never render.
  (3) PT-CT-103 "Top Seal" has inventoryCount -48. (4) 21 parts still tagged
  inventorySource "box_estimate", now a false provenance label.
- Typecheck: 183 errors / 183 warnings = exact pre-existing baseline, zero new;
  no errors in either edited file.
- DEPLOYMENT 2026-07-30: committed d9604550a05590202bc1a5e05afdf5204e7fb915 on
  perf/data-health-01, pushed to origin (avacoder3900/Bioscale_Operations_System_V2)
  -> Vercel build SUCCESS on both projects (brevitest + alejandro-valdezs-projects),
  no spurious failure this time. Preview (branch alias, /login returns HTTP 200):
  https://bioscale-operations-system-mongodb-git-perf-da-5173e4-brevitest.vercel.app
- INVENTORY EDIT 2026-07-30: first direct-to-Mongo part quantity edits since the
  Box disconnect. Via mongosh against prod bioscale, guarded on prior value
  (updateOne matched on {partNumber, inventoryCount: <from>}), all 3 matched:
    PT-SPU-044 Structure Bottom  129 -> 145   _id vsffmZCeL1Nu9mrzE6LSn
    PT-SPU-027 Structure Top      34 -> 129   _id 1ary9W3jNeSRRAn0nOlmj
    PT-SPU-023 Enclosure Back     63 -> 182   _id b3EfKmgXjxm_71vOlszt1
  updatedAt set manually (mongosh bypasses Mongoose timestamps). Pre-edit backup
  of the 3 docs in scratchpad/part_definitions-backup-20260730-113117.json.
  3 matching AuditLog entries written to audit_log, changedBy "alejandrov
  (mongosh)", reason "Manual inventory correction; Box sync removed".
- AUDIT COLLECTION DRIFT: three collections exist - audit_log (34449 docs, LIVE,
  what the AuditLog model binds to), audit_logs (82, stale since 2026-06-23),
  auditlogs (2, stale since 2026-06-22). Write to audit_log only.
- LEDGER INVARIANT DISCOVERED 2026-07-30: part_definitions.inventoryCount is
  DERIVED - it equals the net sum of inventory_transactions.quantity for that
  partDefinitionId (consumption/scrap rows are stored NEGATIVE). Editing
  inventoryCount directly WITHOUT a matching transaction silently breaks this.
  My earlier PT-SPU-044/027/023 edits did exactly that; repaired below.
- BARCODE RECONCILIATION 2026-07-30 (user-reported discrepancy):
  * "PT-SPU-03" resolved to PT-SPU-003 M3 Self-Retaining Washer. User count 273
    CONFIRMED by accepted lots: LOT-20260327-0003 (100) + LOT-20260327-0004 (100)
    + LOT-20260728-0001 (73) = 273. BIMS showed 173 because only 173 units had
    receipt transactions - 100 received units were never transacted. Corrected.
  * PT-SPU-013 Heater Block: user reported lots 17 + 6 = 22 actual. DB DISAGREES -
    lot 01fc1b22-1c28-4671-80bd-688e0ada8ece records qty 12 (not 17), and lot
    c86ba345-89e4-45ab-befa-5c36f808b8d9 DOES NOT EXIST anywhere (all 110
    receiving_lots scanned + generated_barcodes). Set to 22 on user instruction
    ("trust my physical counts"); disagreement recorded in the tx reason field.
  * PT-SPU-078 Laser Diode: user attributed barcode 0f28c6ba-664a-4e89-bedc-
    17340be5cf22 to it, but that barcode is LOT-20260327-0053 belonging to
    PT-SPU-091 LED Light Assembly, qty 10 (not 6). PT-SPU-078 had zero lots and
    zero transactions. Set to 6 on user instruction; mismatch recorded in tx
    reason. PHANTOM LOT c86ba345 NOT created - awaiting user decision.
- WRITES APPLIED (all guarded on prior value, pre-flight checked all 6 before any
  write, backup scratchpad/part_definitions-backup-20260730-131353.json):
    6 x inventory_transactions type "adjustment", performedBy/operatorUsername
    "alejandro" (operatorId JJtUGWAWTnbou3J3isc1), shape copied from the existing
    jacob "manual one-time correction" precedent:
      PT-SPU-044 +16  (129->145, ledger repair, count was already correct)
      PT-SPU-027 +95  (34->129,  ledger repair)
      PT-SPU-023 +119 (63->182,  ledger repair)
      PT-SPU-003 +100 (173->273, count changed)
      PT-SPU-013 +17  (5->22,    count changed)
      PT-SPU-078 +6   (0->6,     count changed)
    3 x audit_log entries for the three count changes (003/013/078).
  VERIFIED: invariant now HOLDS for all 6. Repo-wide 42->46 holding, 11->8 broken
  (remaining 8 are pre-existing, not from this session), 16->15 with no tx.
- SCHEMA DRIFT (inventory_transactions): model enum is
  deduction/retraction/adjustment/receipt, but live data is creation 2453,
  consumption 1911, scrap 199, receipt 101, adjustment 6 - "creation",
  "consumption" and "scrap" are NOT in the enum. Fields operatorId,
  operatorUsername, notes, lotId, manufacturingStep, cartridgeRecordId also
  undeclared. Same strict-mode stripping risk as part_definitions.
